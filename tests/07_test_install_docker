test() {
    # we test for the final line contains the docker command
    # FIXME: test progress here too
    T="docker[[:space:]]+edge[[:space:]]+[0-9.]+[[:space:]]+-[[:space:]]+[0-9a-f]+[[:space:]]+*"    
    test_regexp "$T" sudo snappy install docker

    # ensure its there
    docker 2>/dev/null
}
