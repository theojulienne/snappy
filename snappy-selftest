#!/bin/bash

set -e

. tests/framework

SNAPPY=snappy

MYDIR=$(dirname $0)
for test in $MYDIR/tests/*_test_*; do
    echo "Running $test"
    . "$test"
    if ! OUTPUT=$(test 2>&1); then
        echo "FAILED: $test"
        echo "$OUTPUT"
        exit 1
    fi
done
